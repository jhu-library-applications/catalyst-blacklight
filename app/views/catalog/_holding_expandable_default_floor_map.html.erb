<% unless holding.call_number.blank? or params[:action] == 'copy' %>
  <% if StackmapFetcher.new(holding).map_available? %>
    <%= link_to( {:controller => "map", :action => "index",
                  :collection_code => holding.collection.internal_code,
                  :call_number     => holding.call_number  } ,
                 :class => "get_map btn btn-outline-secondary btn-xs ml-4",
                 :data => {:blacklight_modal => "trigger",:toggle => "modal", :target => "#blacklight-modal"}) do
    %>
      <span class="glyphicon glyphicon-map-marker pr-1"></span>Map It
    <% end %>
  <% end %>
<% end %>
